version: "3.8"

services:
  postgres:
    image: postgres:15-alpine
    container_name: arcfit-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: arcfit
      POSTGRES_PASSWORD: arcfit123
      POSTGRES_DB: arcfit_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - arcfit-network

  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   container_name: arcfit-pgadmin
  #   restart: unless-stopped
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@arcfit.com
  #     PGADMIN_DEFAULT_PASSWORD: admin123
  #   ports:
  #     - "5050:80"
  #   depends_on:
  #     - postgres
  #   networks:
  #     - arcfit-network

volumes:
  postgres_data:

networks:
  arcfit-network:
    driver: bridge
